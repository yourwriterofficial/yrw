# Hardening backlog state (2026-07-13)

Ref: memory/open-workstream-hardening.md (full 11-item list)

## Done
- [x] 1. Nav bar overlap fix (site-wide, safe-area root cause) — verified via forced --safe-t override + screenshot, mobile+desktop
- [x] 2. Alert/notification button — root cause: `notifications` table was never in the
      `supabase_realtime` publication (confirmed empty via SQL), so the bell's postgres_changes
      listener never fired, ever. Fixed via migration `enable_realtime_notifications`
      (`alter publication supabase_realtime add table public.notifications;`) on project
      jaqkqlitaqxjbkxfcvjs — applied live with user confirmation. RLS already correctly scoped
      (auth.uid()=user_id on SELECT/UPDATE). Also fixed a latent cross-user leak: the client
      subscription had no per-user filter, so once realtime worked every signed-in user would've
      gotten a toast for every OTHER user's notification insert too. Added `filter: user_id=eq.<id>`
      + `userId` prop threading in NotificationBell.tsx, admin/layout.tsx, dashboard/client/layout.tsx.

- [x] 3. Transactions logging — audited every purchase/payment path:
      Wallet paths (dev-shop `/api/dev-shop/checkout`, project-topics `/api/projects/checkout`,
      `/api/client/pay-addon-wallet`, `/api/client/pay-milestone-wallet`) already logged to
      `transactions` correctly (prior session's work) — no gap there.
      Real gaps found + fixed in `app/api/paystack/webhook/route.ts`:
      (a) DEPOSIT and BALANCE payment types (standard 60/40 non-milestone orders) never
          inserted into `transactions` at all — only ADDON-/INDEX- did. Added inserts using
          `orderInfo.client_id` + `rawAmountPaid`.
      (b) Custom-invoice milestone payments (CUSTINV_ prefix) never logged to `transactions`
          at all. Added insert with best-effort user_id match via `profiles.email` (custom
          invoices aren't always tied to a platform account), else null with a descriptive
          `notes` field (admin Finance page already renders `tx.notes`).
      (c) The whole ADDON-/INDEX-/DEPOSIT/BALANCE branch had NO idempotency guard against
          Paystack webhook retries — could have double-logged transactions on retry. Added a
          reference-based duplicate check (matches the pattern already used for wallet_topup/
          project_material/dev_product). Same guard added to the custom-invoice branch.
      Verified: `npx tsc --noEmit` clean. Not live-tested (webhook needs a real Paystack HMAC
      signature; this is server-only logic with no browser surface to click through).

- [x] 4. Script shop as real /developer/shop sub-page. Extracted the marketplace grid
      (search/sort/category/BuyModal) out of app/developer/page.tsx into new
      app/developer/shop/page.tsx. /developer is now a slim landing page with a
      "Ready-Made Scripts" teaser card linking to /developer/shop. Updated every
      /developer#shop reference sitewide (app/page.tsx x2, dashboard/client/ScriptsTab.tsx,
      developer/scripts/[slug]/page.tsx x2) to /developer/shop. tsc clean, both routes
      verified rendering in browser (grid + products load on /developer/shop, teaser
      renders on /developer, no console/server errors).

- [x] 6. Dead links sitewide — mechanical sweep (static hrefs, query-string hrefs,
      template-literal hrefs, router.push targets, admin NAV_ITEMS, external/mailto/tel
      links, `href="#"` placeholders) cross-referenced against real file-based routes.
      Found zero dead links — sitewide routing is clean. (One false alarm: duplicate
      `id="topics-grid"` in projects/page.tsx turned out to be two mutually-exclusive
      conditional branches, not an actual DOM duplicate.)
- [x] 5. Client dashboard production-grade audit. Found and fixed a real bug: 4 order
      forms (order/new/{custom,content,dev,resume}/page.tsx) plus the shared academic
      wizard (components/OrderForm.tsx) used native browser alert() for validation/error
      messages — 16 call sites total. Replaced all with the app's existing showToast()
      system. Discovered ToastContainer was mounted ad-hoc per-page (15 admin pages +
      dashboard/client/layout.tsx) and NOT at all on public pages like /order/academic —
      meaning showToast() would've silently done nothing there. Fixed properly: moved
      ToastContainer to app/layout.tsx (mounted once, sitewide, survives client-side
      navigation so toasts persist through router.push), removed the 16 redundant
      per-page mounts to avoid duplicate toasts. tsc clean.
      Flagged but NOT fixed (spawned as background task task_664f0366): dashboard/client/
      page.tsx handlePayment() uses window.confirm() for the wallet-vs-card payment
      choice — real UX smell but a payment-logic-adjacent refactor, out of scope for a
      quick pass; needs its own careful pass with live click-through testing.
      Browser verification was partial: tsc passed and the toast mechanism is proven
      (already used successfully across 15+ existing admin pages), but live click-through
      on /order/academic hit Browser-pane tool flakiness (screenshot timeouts, stale
      read_page trees) — looked like a concurrent session hitting the same dev server
      (unrelated /admin, /developer, /login traffic in server logs while testing).

## Investigated, needs user decision before building (item 10)
- [ ] 10. Affiliate system audit: it's currently a HOLLOW SHELL. Only
      app/admin/affiliate/page.tsx exists — grepped the whole app for "affiliate|referral"
      and found zero other hits. There is NO client-facing entry point at all:
      - No referral code/link generation for clients (no `profiles.referred_by` or
        referral-code column — checked live schema via Supabase MCP)
      - No capture of a referral on /login or /register (nothing reads a `?ref=` param)
      - No code anywhere INSERTs into `referrals` (commission never actually gets
        calculated/recorded on any real order)
      - No code anywhere INSERTs into `withdrawals` (the admin approve/reject UI has
        nothing to act on — a client can never actually request a payout)
      Admin can only configure commission %% / min withdrawal and approve/reject
      withdrawal requests that no client can ever create. This is a real feature-build,
      not a bug fix: needs a referral-code scheme (could piggyback on the profiles.permalink
      column meant for item #8 usernames), signup/login capture wiring, commission
      crediting logic hooked into the Paystack webhook + wallet payment paths, a new
      client dashboard tab (referral link, earned commissions, request-withdrawal form),
      and a new API route for withdrawal requests. Deliberately NOT built blind this
      session since it touches money-crediting logic and the auth flow — flagged for the
      user to scope (referral code format, auto-credit vs admin-approval-per-order, etc.)
      before implementing.

- [x] 4 + 10. Usernames/permalinks + affiliate system, built end-to-end per user's
      confirmed decisions (username IS the referral code; commission auto-credits to
      wallet on order payment).
      DB migration `add_referred_by_and_permalink_autogen` applied on jaqkqlitaqxjbkxfcvjs:
      - profiles.referred_by uuid (nullable, references profiles.id)
      - unique case-insensitive index on profiles.permalink
      - `assign_profile_permalink` BEFORE INSERT trigger — auto-generates a unique
        slug from full_name/email for every new profile, any signup path
      - backfilled all 11 pre-existing profiles that had no permalink
      New code:
      - lib/permalink.ts — format validation + slugify (diacritic-stripping tested)
      - lib/affiliate.ts — creditReferralCommission() (server-only, credits referrer's
        wallet + logs a `referrals` row + a `transactions` row of type
        'referral_commission') and applyReferralIfEligible() (one-time-only
        profiles.referred_by linkage, resolved from a permalink)
      - app/components/ReferralCapture.tsx — captures ?ref=<username> into
        localStorage (reads window.location directly, not useSearchParams, to avoid
        forcing Suspense boundaries on static pages), mounted once in app/layout.tsx
      - app/register/page.tsx — applies stored ref code via new
        /api/auth/apply-referral route (accepts userId in body, not session — signUp()
        has no session yet pre-email-confirmation); Google OAuth path appends ?ref= to
        the redirectTo URL, resolved in app/auth/callback/route.ts
      - creditReferralCommission wired into every real "payment"-type transaction
        insert: Paystack webhook (project_material, dev_product, ADDON-, INDEX-,
        DEPOSIT, BALANCE branches — custom-invoice payments deliberately excluded,
        buyer often isn't a tracked platform account), plus the wallet-payment routes
        (/api/projects/checkout, /api/dev-shop/checkout, /api/client/pay-addon-wallet,
        /api/client/pay-milestone-wallet)
      - NEW: /api/client/pay-order-deposit-wallet route — replaces OrderForm.tsx's
        old client-side wallet-deposit debit (which called increment_wallet directly
        from the browser). Had to move this server-side: increment_wallet() refuses
        to CREDIT any wallet for a non-service-role caller by design (checked its
        actual SQL definition), so referral-commission-crediting can only ever run
        from trusted server code, never client JS — this was a real security
        consideration, not just refactoring taste.
      - NEW: /api/client/request-withdrawal — client requests a payout; debits wallet
        immediately (so it can't be double-spent while pending), inserts a
        `withdrawals` row.
      - NEW: /api/admin/process-withdrawal — approve/reject a payout. Rejecting
        REFUNDS the wallet (admin's old direct-Supabase-call handler couldn't have
        done this even if written to: crediting requires service-role, browser-side
        admin calls can't). Updated app/admin/affiliate/page.tsx to call this instead
        of updating the withdrawals row directly.
      - NEW: app/dashboard/client/AffiliateTab.tsx — referral link + copy button,
        editable username, stats (wallet balance / total earned / people referred),
        withdrawal request form, payout history. Wired into page.tsx + layout.tsx nav
        (desktop + mobile) as a new "affiliate" tab.
      Verified: tsc clean throughout. Confirmed DB state post-migration (backfilled
      permalinks correct). Confirmed slugify diacritic-stripping via node script.
      Confirmed ?ref= capture works live in browser (localStorage set correctly on
      /register?ref=juderichlord). Did NOT do a full live click-through of an actual
      referred signup + purchase + commission-credit + withdrawal-request +
      admin-approve cycle — that needs real test accounts and would consume
      significant remaining turn budget; recommend a dedicated smoke-test pass before
      relying on this in production.

- [x] Ad-hoc fix (user-reported): "My Scripts" tab only showed purchased scripts,
      with no way to discover/buy more without leaving to /developer/shop. Rewrote
      app/dashboard/client/ScriptsTab.tsx to add a "More From the Store" section below
      the owned-scripts grid, showing all active dev_products the user hasn't already
      purchased (category filter chips, same card style as /developer/shop), with an
      inline BuyModal. Added an `onPurchased` callback to app/developer/BuyModal.tsx so
      a wallet purchase immediately refreshes the owned/available split in place
      (previously the modal just told users to go check "My Scripts" — awkward when
      already there). tsc clean.

## Admin UI/UX revamp (item 3) — in progress
- [x] Sub-fix 1 DONE: grouped the 15 flat admin sidebar nav items into 6 labeled
      sections (Overview / Sales & Orders / Catalog / People / Communication / System)
      in app/admin/layout.tsx, wired into BOTH desktop sidebar and mobile dropdown menu
      with section-header rendering. Also fixed the header breadcrumb title — it used
      to derive the page name by crudely munging the URL path
      (`pathname.replace('/admin/', '').replace('-', ' ')`, non-global replace so
      multi-hyphen slugs would render wrong), now does a proper NAV_ITEMS label lookup
      via `currentPageLabel`, shown as "Admin > {label}". tsc clean.
      This was the single biggest structural contributor to the "scattered" complaint
      since it's the shell every admin page sits inside.
      Still pending for item 3: nothing else started yet (visual polish beyond the
      shell, per-page consistency pass).

- [x] CRITICAL PERF FIX (user-reported, ad-hoc): /developer, /resume-cv,
      /content-writing, /academic-writing (and every other public route NOT in a
      4-item hardcoded allowlist — /projects, /projects/[id], /developer/shop,
      /developer/scripts/[slug], /custom-research, /order/*) were hitting a BLOCKING
      supabase.auth.getUser() network call in proxy.ts on EVERY single request, even
      though the result is only ever consulted for /dashboard and /admin paths.
      Confirmed in this session's own dev server logs: proxy.ts alone took up to
      6.8 SECONDS on some requests (GET /developer/shop: proxy.ts 6.8s). Fixed by
      skipping the auth check entirely unless `path.startsWith('/dashboard') ||
      path.startsWith('/admin')` — removed the fragile hardcoded publicPaths array
      (which silently missed every page not explicitly listed, including every
      dynamic route) in favor of gating based on what actually NEEDS the check.
      Verified: proxy.ts now takes 6-30ms consistently (down from
      up to 6.8s) via server logs; pages still render correctly (full Header/shell
      present, content correct) via browser. tsc clean.
      This was almost certainly the root cause of BOTH complaints in the same message
      ("slow" AND "doesn't render professionally" — a multi-second blocking delay
      before a static marketing page paints looks exactly like "not rendering
      professionally").

- [x] Admin revamp sub-fix 2: 4 of 15 admin pages (affiliate, logs, settings,
      transactions) had ad-hoc hand-rolled headers instead of the shared PageHeader
      component (11 others already used it) — a real "scattered" inconsistency.
      Converted all 4 to PageHeader with breadcrumb + icon + actions slot.
      Also found and fixed a real duplicate-rendering bug while in settings/page.tsx:
      it had its OWN complete reimplementation of the toast system (local `toastId`
      counter, local `showToast` dispatching the same `'app:toast'` CustomEvent, local
      listener + local render block) — a leftover from before the app had a shared
      Toast module, now fully redundant since ToastContainer is global (root layout).
      Any showToast() call anywhere in the app while viewing /admin/settings would
      have rendered TWICE. Deleted the local reimplementation entirely, now uses the
      shared `showToast` import like everywhere else.
      admin/chat/page.tsx (5th non-conforming page) deliberately left alone — it's a
      full-height two-pane messaging UI where a standard PageHeader block would eat
      needed vertical space; a legitimate exception, not an inconsistency.
      tsc clean, no server errors.

- [x] Item 9 (reduce complexity) sub-fix 1: applied the same nav-grouping treatment
      from the admin sidebar revamp to app/dashboard/client/layout.tsx — Vault/Wallet/
      Affiliate/Profile were 4 flat items alongside "Writing Services" and "Project
      Materials" groups; grouped them under a new "Account" section header (desktop +
      mobile), keeping Support Chat standalone as it's not an account-settings item.
      tsc clean.

- [x] REAL BUG FOUND WHILE VERIFYING (caught via live concurrent-session crash in
      server logs, not something I introduced): all 4 order/new/{dev,custom,content,
      resume}/page.tsx files had a race condition — `fetchUser()` and `fetchConfig()`
      run in two INDEPENDENT useEffects, and `setLoading(false)` was tied only to
      fetchConfig() finishing, not fetchUser(). If the config queries (order_addons,
      site_content) resolved before the auth check, the loading guard cleared while
      `user` was still null, crashing on an unguarded `user.email` access
      ("Cannot read properties of null (reading 'email')" — confirmed in this
      session's own server logs, at app/dashboard/client/order/new/dev/page.tsx:193).
      Fixed all 4 files: `if (loading)` → `if (loading || !user)`. Checked the 5th
      order page (academic) — it just wraps the shared OrderForm.tsx, which already
      guards correctly via a single `isLoaded` flag and legitimately supports guest
      (logged-out) checkout, so no bug there. tsc clean.

- [x] Item 9 sub-fix 2: OrderForm.tsx (the academic order wizard, shared by /order/academic
      and the logged-in dashboard academic flow) crammed pricing plans + personal details
      + billing + a 15-option Citation Style dropdown + a 15-option Font Preference
      dropdown ALL into step 1 of 5 — the single biggest concrete "too complex" signal
      found in the client-facing flows. Both selects already had sensible defaults
      (APA 7th Edition / Times New Roman 12pt) that cover the vast majority of orders,
      so the complexity was purely visual, not functional. Collapsed both selects behind
      a "Formatting Requirements" disclosure toggle, collapsed by default, showing
      "Using default: {refStyle} · {fontStyle}. Tap to customize." — no options removed,
      just not dumped in front of every user by default. Verified rendering correctly
      via browser (collapsed summary line shows exactly as intended). tsc clean.
      Deliberately did NOT touch the actual citation-style/font CONTENT (many are
      Nigerian-institution-specific formats like Unilag LCM, OAU Law Format, ABU Thesis
      Guide — clearly deliberate catalog depth for the target market, not arbitrary
      bloat; cutting them would remove real business value, not reduce complexity).

- [x] Item 7 (mobile/iOS polish) sub-fix: the 3 site-wide floating action buttons
      (WhatsApp bubble in app/layout.tsx, OrderAssistant, ProjectsAssistant) all used
      raw `bottom-6` with no iOS safe-area accounting — same class of gap as the item
      #1 nav-overlap fix, just for bottom-pinned elements instead of top. Added a
      `.fab-bottom-safe` utility (`bottom: calc(1.5rem + env(safe-area-inset-bottom))`)
      to globals.css and applied it to all 5 usages across the 3 files (button + open
      panel for each of OrderAssistant/ProjectsAssistant). tsc clean.
      Verified NOT an overlap bug (checked before touching): OrderAssistant.tsx already
      has `if (pathname?.startsWith('/admin') || pathname?.startsWith('/projects'))
      return null;` specifically so it never coexists with ProjectsAssistant on
      /projects* routes — both render at the identical bottom-6/left-6 spot by design,
      intentionally mutually exclusive, not a stacking bug.

- [x] Global subjective-polish pass: verified visual quality directly in browser
      (homepage light+dark, /developer/shop) rather than guessing from code —
      confirmed the design system is already solid (focus-visible accessibility
      states already global in globals.css:207-219, btn-primary/secondary utilities
      exist though most of the codebase uses bespoke per-accent Tailwind combos as
      the actual dominant convention — not an inconsistency, just how this codebase
      already works). No fake inconsistencies invented.
      REAL FINDING while looking: all 3 products in the dev-shop catalog were
      "[TEST] ..." placeholder listings (Subskriptions Starter Kit, YourResearchWriter
      Clone Kit, Admin Dashboard Boilerplate) — `is_active: true`, live and BUYABLE on
      the public /developer/shop, but none had an actual source file
      (`file_path IS NULL`) to deliver. A real customer could have paid (wallet or
      card) for a script that literally cannot be downloaded. Confirmed with user via
      AskUserQuestion, then set `is_active = false` on all 3 via Supabase MCP
      (reversible, not deleted). Verified: shop now shows the correct "No scripts
      available in this category yet — check back soon" empty state instead of fake
      buyable products.

- [x] Followed up the dev-shop test-product finding with a sitewide sweep for other
      live test/placeholder catalog data: project_topics (2 title matches for "test" —
      both legitimate academic topics about software testing automation, false
      positive, no action), project_addons, order_addons, promo_codes — all clean.
      Confirmed the dev-shop was the only affected area.

- [x] Deep performance audit ("make it super fast" ask). Ran a real `next build` +
      `next start` production test (dev-mode Turbopack timings are noise, not
      representative) — confirmed EVERY key public page now responds in 18-25ms
      server-side (down from up to 6.8s pre-proxy.ts-fix). Checked and ruled out
      further big wins:
      - Bundle size: 2.3MB total client chunks, code-split per route, reasonable for
        the feature set — not bloated.
      - `import * as lucide from 'lucide-react'` (used in 30 files): checked Next.js's
        own docs — lucide-react is already in Next's default `optimizePackageImports`
        list, meaning it's auto-tree-shaken regardless of import style. Confirmed NOT
        an issue; did not "fix" something already handled by the framework.
      - Root layout.tsx: no blocking async/await — confirmed optimal, doesn't hold up
        every page's SSR.
      - Static generation: all pure-content pages (service landing pages, /projects,
        /developer, order forms) already prerender as static (○) per the build output;
        only genuinely dynamic per-item routes (/projects/[id], /developer/scripts/
        [slug], /invoice/[id]) are server-rendered on demand — already optimal.
      Found and fixed one real remaining gap: next/image was used ZERO times
      site-wide despite next.config.mjs already being configured for AVIF/WebP +
      Supabase remotePatterns. Converted all 6 raw `<img>` tags (dev-shop preview
      thumbnails across admin/dev-shop, dashboard ScriptsTab x2, developer/shop,
      developer/scripts/[slug] x2 — gallery + thumbnail strip) to `next/image` with
      `fill` + proper `sizes`. Had to add `relative` to two parent containers that
      were missing it (required for `fill` to position correctly). Low CURRENT
      traffic impact (dev-shop catalog is empty right now, see [[dev-shop-feature]])
      but correct groundwork for when real products/screenshots get added.
      Verified: tsc clean, full production build succeeds cleanly both before and
      after the image conversions.

- [x] Admin/global polish (option 3, continued): found 3 separately-duplicated local
      `StatCard` components (app/admin/page.tsx, app/admin/orders/page.tsx,
      app/dashboard/client/page.tsx) — same conceptual "stat tile" pattern, each with
      slightly different visual treatment (icon-above vs icon-top-right vs no-icon;
      `color` prop affecting value text in 2 of 3 but only the icon wrapper in the
      3rd). Consolidated into one shared `app/components/ui/StatCard.tsx` (icon
      optional, color affects both icon wrapper + value text — matches the admin
      dashboard's version, the most-used/most-visible instance) and updated all 3
      call sites to import it, deleting the local duplicates.
      REAL BUG FOUND in the process: admin/orders/page.tsx's local StatCard defaulted
      to `color = "text-white"` — hardcoded white text, not the theme-aware
      `text-primary` class. In light mode this would render near-invisible (white
      text on a light card) for its "Total Orders" and "Pipeline Value" tiles (the
      two that don't pass an explicit color). The shared component's `text-primary`
      default fixes this as a side effect of the consolidation.
      tsc clean, verified twice.

- [x] Mobile touch-target audit (item 7, continued): found the 3 primary mobile
      hamburger-menu buttons sitewide (site Header.tsx, admin/layout.tsx,
      dashboard/client/layout.tsx) all sized ~40px (p-2 + 24px default lucide icon) —
      just under the standard 44px minimum tap-target guideline. Bumped all 3 to
      p-2.5 (exactly 44px). Also bumped the smaller back/close buttons inside the
      OrderAssistant/ProjectsAssistant chat widgets from p-1.5 (28px) to p-2.5 (36px)
      — didn't go all the way to 44px there since it would visually overwhelm that
      compact chat-header row, but meaningfully larger (+28% tap area).
      tsc clean, verified twice.

      NOTE: a full `next build` re-verification of these last two rounds (StatCard
      consolidation + touch-target bumps) hit persistent Bash-tool "model temporarily
      unavailable for safety classifier" flakiness — retried 5+ times, light commands
      (echo, tsc) worked fine but `npm run build` specifically kept failing at the
      classifier layer. tsc passed clean twice and the last FULL production build
      (before these two rounds) was clean, and both rounds are simple/mechanical
      (a component consolidation already proven working elsewhere, and Tailwind
      padding-class bumps) — proceeding on that basis rather than blocking further.
      If picking this up again: re-run `npm run build` once to confirm.

## In progress
- [ ] (none — pick next from Pending)

## Was in progress (reference only, see Done above)
- 1. Nav bar overlap fix (site-wide, safe-area root cause)
      Root cause: Header is `fixed`, adds `pt-safe` (env(safe-area-inset-top)) internally.
      Pages compensate with static rem pt-* (pt-20/24/28/32/36/40) that do NOT include
      env(safe-area-inset-top) -> overlap on notch/Dynamic Island iOS devices.
      Fix: add safe-area-aware utility class(es) in globals.css, replace ad-hoc pt-*
      after every <Header/> usage across: app/page.tsx, app/projects/page.tsx,
      app/projects/[id]/page.tsx, app/developer/page.tsx, app/developer/scripts/[slug]/page.tsx,
      app/components/ServiceLandingTemplate.tsx (covers 4 service pages).
      Admin/client dashboard use `sticky` bars (not fixed) - separate check needed for
      safe-area padding on their mobile topbars (line ~133 admin/layout.tsx, ~243 dashboard/client/layout.tsx).

## Pending (not started)
2. Alert/notification button works correctly
3. Transactions logging for project-topic + dev-shop purchases (wallet + Paystack) - verify wallet path
4. Script shop as real /developer/shop sub-page (currently #shop anchor)
5. Every client-view page production grade
6. Delete dead links sitewide
7. Admin UI/UX revamp (scattered -> coherent)
8. Usernames as permalinks
9. Reduce complexity globally (landing/front/user pages)
10. Affiliate system works via login + signup
11. Mobile/desktop/iOS polish sitewide

## Already-uncommitted work found at session start (prior session, not yet committed)
- Header.tsx rewritten (mobile bottom-sheet drawer, service dropdown)
- app/layout.tsx: added viewportFit: "cover" (needed for env(safe-area-inset-*) to work)
- admin nav: added Dev Shop link; client dashboard: added Scripts tab
- Paystack webhook: +95 lines (likely dev_product/transactions logging work)
- app/page.tsx, projects/page.tsx, order/* pages: padding/layout tweaks
- New untracked: app/academic-writing/, app/admin/dev-shop/, app/api/dev-shop/,
  app/components/HomeStats.tsx, ServiceLandingTemplate.tsx, app/content-writing/,
  app/custom-research/, app/dashboard/client/ScriptsTab.tsx, app/developer/, app/resume-cv/
