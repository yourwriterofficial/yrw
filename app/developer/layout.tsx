export const metadata = {
  title: 'Full Stack & Custom Software Development | ResearchWriter',
  description: 'Web applications, mobile builds, APIs, database design, and MVP delivery — plus a marketplace of ready-made scripts with complete source code and IP transfer.',
};

export default function DeveloperLayout({ children }: { children: React.ReactNode }) {
  return children;
}
